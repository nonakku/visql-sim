# Visual SQL Simulator (visql-sim) - AI 開発憲章

本ファイルは、AIアシスタント（Antigravity）がこのプロジェクトで作業を行う際に、自身の行動や出力を厳格に制御するための絶対的な行動指針です。
AIはすべての作業ターンの開始時に必ず本ファイルを読み込み（`view_file`）、例外なくこれを遵守しなければなりません。

---

## 【最優先哲学】推測の完全排除と「実際の描画（事実）」による検証

「コードが正しそうだから動くはずだ」という推測に基づく改善は、手戻りとバグを生む「最も悪なる行為」である。
Webアプリケーションの開発、特にUIのレイアウトや各種ビジュアルUI要素（ブロックエディタや動的コンポーネント等）においては、**「実際にブラウザで描画された事実」と「実行時コンソールログの事実」を確認しない限り、いかなる変更も完了とみなしてはならない。**

AIはコードの文字面だけで満足せず、必ず実画面の描画結果とエラーログという「客観的事実」を捉えてから次の一歩を踏み出すこと。

---

## 遵守すべき7つの厳格ルール

### 1. 主観的な「成功宣言」および誇張表現の完全禁止
- 「完璧に」「100%確実に」「完全に修正しました」といった、主観的または過剰に確信を持たせる表現（Superlatives）を一切使用してはならない。
- 報告する内容は常に客観的な事実（実行したコマンド、適用した差分、得られた実画面ログ）のみに限定し、成功か否かの最終判断はユーザーと実行環境の事実に委ねること。

### 2. 確証的ブラウザ検証プロセス（3段階の実機動作確認）の義務化
UI/UX、CSS、DOM、または各種UIコンポーネントの構造定義などのUIに関わる変更を行った場合、AIは単なるコンソールエラーの有無に留まらず、以下の「3段階の確証検証」を漏れなく実行・記録しなければならない。

1. **デプロイ同期とキャッシュ排除の徹底**:
   - プッシュ後、デプロイ完了までのタイムラグ（目安2分以上）を考慮し、必ずデプロイURLが最新 of コミットハッシュを反映していることを確認してからテストを開始すること。
   - ブラウザの古いアセットキャッシュによる「誤検知（古いエラーコードの誤認や、修正前のコードでのテスト）」を防ぐため、テストは必ずシークレットブラウザ（Incognitoモード）または「キャッシュ無効化（Disable Cache）」を設定して実施すること。

2. **シナリオベースの全経路インタラクション走査**:
   - アプリケーションが提供する主要なユーザー操作の「全経路」を実際にクリック・入力して検証すること。
   - インタラクティブ要素においては、「アコーディオン、ツールボックス、タブ、ドロップダウン等のすべての非表示領域を個別にクリックして展開し、すべての要素が画面内に歪みなく描画・整列されること」を検証の必須項目とする。
   - コア機能の実行・連携においては、実際に「入力の作成・インポート ➔ 処理実行 ➔ 結果/変化表示の描画」までの一連のユーザー体験をシナリオとして完遂し、動作の正しさを事実として担保すること。

3. **エビデンス（画像・録画）の明示と自動蓄積**:
   - 検証完了時は、単に「動作確認しました」とテキストで報告するのではなく、「各状態のスクリーンショット」および「実行セッションの録画ビデオ」を必ず取得すること。
   - 取得したエビデンスは、必ず会話の artifacts フォルダ（または `walkthrough.md`）に整理し、ユーザーが一目で事実を確認できるリンク形式で提示すること。

### 3. トリッキーな設計の排除と Vanilla/Simple JS の維持
- 動作遅延の回避やインスタンス共有のために、予期せぬ挙動を招くトリッキーな設計（例: 不必要な Proxy の導入、過度に複雑な Live Binding 等）を独断で導入しないこと。
- 基本的に、標準的で誰が見ても明快な Vanilla JavaScript、素直なDOM操作、および標準的なライフサイクル設計を徹底すること。

### 4. 課題の「一行ずつの追跡」とハルシネーションの防止
- エラーが発生した場合、問題の核心から目を背けて別のアプローチへ飛び火したり、関連のないコードを大量に書き換えたりしてはならない。
- エラーメッセージのスタックトレースを正確に読み解き、対象ファイルの該当行をピンポイントで確認し、エラーの根本原因を一行ずつ冷静に説明・修正すること。

### 5. 専用ツール・コマンドの戦略的推奨プロセス
- **エラーが発生している時**、またはテスト検証、仕様のすり合わせ、デバッグなど、専用のツールやコマンドを用いたほうが客観的かつ効率的であると判断される場合、AIは単独で思考や作業を抱え込まず、速やかに適切なスキルやスラッシュコマンド（例: `/browser` による画面上のエラー・コンソールログの抽出、`/grill-me` による設計不整合の解決、`/goal` による自律的なエラーの自動復旧）の利用をユーザーに具体的に提案すること。
- ただし、不要な提案によるノイズ化を防ぐため、エラー発生の瞬間、コードのプッシュ直後、または設計プランニング時などの「明確な問題発生時・転換点」に限定して提案を行うこと。

### 6. デザインシステム（Google Minimal）の厳格な維持
- **絵文字の完全排除**: ヘッダー、アコーディオン、ボタン、プレビュー領域など、UIのあらゆる場所から絵文字（例: 🏢, 🖼️, 🟢）を完全に排除し、アイコンはすべて `Google Material Symbols (Outlined)` に統一すること。
- **ブランドカラーの制限**: 使用するブランドカラーは Google Blue (`#1a73e8`) の1色のみに制限し、安易なネオンカラー、過度なグラデーション、多色使いを一切禁止すること。
- **テーマの統一**: Google仕様のミニマルなライトテーマを厳守し、ダークモードやサイバーパンク風デザインへの変更を禁止すること。

### 7. 開発憲章の定期的レビューと継続的進化（Living Document）
- 本憲章は、一度作成して固定される「過去の遺物」にしてはならない。開発が進み、新たなバグやアーキテクチャ上の学び、環境固有の制約が発見されるたびに、本ファイルを能動的に見直し、アップデートしていかなる再発も防ぐこと。
- 大きな課題の解決後やマイルストーンの達成時には、AIから自発的に「今回の教訓を憲章ファイルに蓄積し、進化させるべきか」をユーザーに確認し、常に実態に即した最新のルールブックであり続けること。
- **【改定の絶対条件（最重要）】**: **AIが本憲章（`DEVELOPMENT_RULES.md`）の内容を独断で書き換えたり、勝手に修正・コミットしたりすることは厳重に禁止する。** ルールの改定や追記の必要性がある場合は、必ず事前に具体的な改定案（差分）を人間に提示して確認を求め、明示的な承認を得られた場合にのみ書き換えを実行すること。

---

## 開発作業時の確認手順（AI自己チェック用）
1. [ ] 作業ターンの開始時に、本ファイル（`DEVELOPMENT_RULES.md`）を読み込んだか？
2. [ ] 変更後の検証時、GitHub Actionsのデプロイ完了（プッシュ後約2分）を待ってからブラウザを起動したか？
3. [ ] テストブラウザはキャッシュを完全に無効化（Incognito等）して起動したか？
4. [ ] すべてのインタラクティブな操作要素（カテゴリ、アコーディオン、メニュー等）を走査し、全パーツがエラーなく描画されることを目視確認したか？
5. [ ] 実際にコア機能の一連の操作を実行し、結果（出力ビューや動的変化）が表示されるまでの正常シナリオを確認したか？
6. [ ] 得られたエビデンス（スクリーンショット、ビデオ）を `walkthrough.md` に登録・リンクしたか？
7. [ ] 報告文の中に、客観的事実以外の「主観的な保証表現」が含まれていないか？
