# Visual SQL Simulator (visql-sim) - AI 開発ルール

本ファイルは、AIアシスタント（Antigravity）がこのプロジェクトで作業を行う際に、自身の行動や出力を厳格に制御するための行動指針です。
AIはすべての作業ステップの開始時に、必ずこのファイルを読み込み（`view_file`）、以下のルールを最優先事項として遵守しなければなりません。

---

## 遵守すべき5つの鉄則

### 1. 主観的な「成功宣言」および誇張表現の完全禁止
- 「完璧に」「100%確実に」「完全に修正しました」といった、主観的または過剰に確信を持たせる表現（Superlatives）を一切使用してはならない。
- 報告する内容は常に客観的な事実（実行したコマンド、適用した差分、得られたログ）のみに限定し、成功か否かの最終判断はユーザーと実行環境の事実に委ねること。

### 2. 事実ベース・ログベースの検証の徹底
- コード変更後、ただ「直したはずである」という推測のみで完了報告を行ってはならない。
- ローカルビルドコマンド（例: `npm run build`）の実行結果、またはブラウザのデベロッパーコンソール等のエラーログなど、客観的な証拠が得られた範囲のみを正確に報告すること。
- ローカルで実行できない制約がある場合は、その制約（「ここではデプロイ後の実ブラウザ確認ができないため、静的解析とビルド検証のみを行いました」など）を明記し、過剰な保証を避けること。

### 3. トリッキーな設計の排除と Vanilla/Simple JS の維持
- 動作遅延の回避やインスタンス共有のために、予期せぬ挙動を招くトリッキーな設計（例: 不必要な Proxy の導入、過度に複雑な Live Binding 等）を独断で導入しないこと。
- 基本的に、標準的で誰が見ても明快な Vanilla JavaScript、素直なDOM操作、および標準的なライフサイクル設計を徹底すること。

### 4. 課題の「一行ずつの追跡」とハルシネーションの防止
- エラーが発生した場合、問題の核心から目を背けて別のアプローチへ飛び火したり、関連のないコードを大量に書き換えたりしてはならない。
- エラーメッセージのスタックトレースを正確に読み解き、対象ファイルの該当行をピンポイントで確認し、エラーの根本原因を一行ずつ冷静に説明・修正すること。

### 5. 作業開始時のルール確認プロセス
- すべての作業ターン、または実装フェーズの開始時において、AIは必ず本ファイル（`DEVELOPMENT_RULES.md`）を読み込むこと。
- 思考プロセス（Thought）において「開発ルールを確認した」旨を記録し、自己の行動がルールに合致しているかを各ステップでセルフチェックすること。
